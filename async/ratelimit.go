package async

func RateLimitPrint() {
	// wg := &sync.WaitGroup{}
	// quotaCh := make(chan struct{}, quotaLimit)
	// for i := 0; i < goroutinesNum; i++ {
	// 	wg.Add(1)
	// 	go startWorker2(i, wg, quotaCh)
	// }
	// time.Sleep(time.Millisecond)
}
